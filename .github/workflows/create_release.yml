name: CI
on:
  push:
    paths:
    branches: [ main ]
  workflow_dispatch:
jobs:
  add-description:
    name: Adding 
    runs-on: ubuntu-latest
    steps:
      - name: Adding the description
        run: |

          curl  -H "Authorization: ${{  secrets.GITHUB_TOKEN }}" \
                --request PATCH \
                --data '{"name":"P008165", "description":"tashi delek"}' \
                https://github.com/ta4tsering/P008165
                



